{% block content %}
    <div class="container-fluid col-8">
        {{form_start(form, {method: 'POST', action: path('create_comment', {'slug': trick.slug})}) }}
            <h5 class="mt-3">Commenter</h5>
            <p>{{form(form)}}</p>
        {{form_end(form)}}
    </div>
{% endblock %}
